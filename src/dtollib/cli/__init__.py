"""Command-line tools.

Five entry points, each wired in ``pyproject.toml``:

- ``dtol-discover`` — enumerate attached boards and subsystems.
- ``dtol-diag`` — diagnose DataAcq SDK / DLL / driver install issues.
- ``dtol-capture`` — short hardware-clocked acquisition to file.
- ``dtol-read`` — one-shot scalar read from an analog-input channel.
- ``dtol-info`` — full per-board capability dump.
"""
