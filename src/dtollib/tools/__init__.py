"""Offline tools — replay, conversion, format inspection.

Currently ships:

- :mod:`dtollib.tools.replay_raw` — read ``.dt-raw`` v2 files back as
  :class:`DaqBlock` iterators for post-hoc analysis.
"""
